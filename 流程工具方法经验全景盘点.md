# 流程工具方法经验全景盘点

## 目标

这份盘点不再重复讲“本项目能做什么”，而是集中回答四个问题：

1. 当前项目已经形成了怎样的流程观
2. 当前项目实际依赖了哪些工具链与控制工具
3. 当前项目的方法论主轴到底是什么
4. 这一路演进下来，哪些经验已经被证明有效，哪些弯路已经被证明不该再走

本稿尽量按仓库当前真源收口，而不是按历史对话印象收口。

## Findings

- 当前项目已经不再适合被理解成“一个会审论文的 prompt 集合”，更准确的定位是：一个把论文评审、证据绑定、导师建议和运行控制拆层管理的审查操作系统。
- 当前主流程已经从 `prompt -> answer` 转成了 `review -> verdict -> advice`。导师职责不再是与评审并列的自由输出面，而是评审闭环后的受控下游。
- repo 级流程与 paper 级 runtime 已经显式分层；`workflow/review-workspace/` 与 `workflow/skill-maintenance/` 两套 truth pack 的存在，说明项目已经接受“业务流程”和“skill 维护流程”必须分家这件事。
- 当前工具链不再只是“帮忙提取材料”的辅助链条，而已经承担了对象层建立、gate 拦截、toolchain 评估、workflow 回归和 truth pack 漂移检查等控制职责。
- 当前方法论最成熟的部分不是“多说几个专家角色”，而是四件事：评审优先、对象层优先、政策化输出授权、热冷路径收口。
- 当前经验面最值得保留的结论是：真正有效的升级，几乎都来自合同化、脚本化和回归化；几乎所有代价高的偏差，都来自第二真源、过度投影或把说明文档错当运行真源。

## 流程全景

### 1. 使命层级

当前项目已经把使命层级定得比较清楚：

1. 先完成高水平评审
2. 再进入导师职责

这不只是口头原则，已经落在多个层面：

- [SKILL.md](/Users/jixiaokang/.agents/skills/graduate-thesis-review/SKILL.md)
- [README.md](/Users/jixiaokang/.agents/skills/graduate-thesis-review/README.md)
- [workflow/review-workspace/WORKFLOW.md](/Users/jixiaokang/.agents/skills/graduate-thesis-review/workflow/review-workspace/WORKFLOW.md)
- [governance/design/2026-04-files-driven-workflow-multirole-design.md](/Users/jixiaokang/.agents/skills/graduate-thesis-review/governance/design/2026-04-files-driven-workflow-multirole-design.md)

这里最关键的流程判断不是“先看论文再提建议”，而是：

- `review` 可以独立存在
- `advice` 不能脱离 `review` 单独成立
- 高风险简单请求会被提升回 `review-first`

这意味着本项目已经不再接受“摘要改写、结论润色、答辩口径、图表结论提炼”被当成低风险局部任务直接放行。

### 2. 双 Workflow 分家

当前 repo 里至少已经形成两条不同的流程线：

1. `workflow/review-workspace/`
   - 管单篇论文评审工作区的控制边界
2. `workflow/skill-maintenance/`
   - 管 skill 自身维护、审计、发版和 release 证据

这件事的重要性在于：

- 以前“审论文流程”和“维护 skill 的流程”容易混在同一仓库根目录和同一套说明资产里
- 现在仓库已经明确承认两者不是一条线，不能共用一个主流程叙述

这也是本项目流程成熟度明显高于很多“只有一个 README 和一个大 prompt”的 skill 的地方。

### 3. Authority Map 已收口

当前 authority map 的主干已经比较清楚：

1. repo truth
   - `workflow/review-workspace/`
   - `workflow/skill-maintenance/`
2. paper runtime
   - `papers/<paper-id>/governance/review-workspace-pack/`
3. paper evidence
   - `papers/<paper-id>/evidence/`
4. derived outputs
   - `papers/<paper-id>/outputs/`
5. supplemental notes
   - `papers/<paper-id>/notes/`

这背后的流程含义是：

- repo 真源不直接拿来做运行目录改写
- 单篇论文要有自己的 runtime pack
- 证据层与输出层分开
- note-like 文件保留，但不再让 Markdown 独占运行权威

这条 authority map 是当前流程观的基础，不是附属细节。

### 4. 默认热路径与显式冷路径

当前项目没有继续走“大而全默认加载”，而是把默认热路径压到了 4 个核心模块：

1. `review-rubric`
2. `specialty-router`
3. `specialty-manual-readiness-gate`
4. `review-operations-architecture`

其余能力，如导师式逐条改稿、前沿检索、深审加闸、形式审查、展示投影、跨代理适配，都被降成了冷路径，按触发条件加载。

这套设计说明当前流程观已经接受一个很重要的现实：

- 强能力不等于首屏全展开
- 机器合同要保留
- 用户心智负担必须收口

### 5. 请求升级与阶段纪律

本项目现在的流程不是“用户说改哪段就改哪段”，而是先判断这个请求会不会穿透证据边界。

已经比较稳定的升级规则包括：

- 高风险简单请求回退到 `review-first`
- `handoff_resume` 才强制 `process_projection`
- `specialty_readiness = partial / missing` 时必须补论文级专项说明
- 局部改写例外只能以受控任务卡存在，不能偷偷膨胀成全文放行

这说明项目已经从“线性步骤表”转向“带 route / gate / exception 的受控流程”。

### 6. 输出授权链已经迁移

当前导师/学生输出授权的第一判断面，已经不是 legacy `release_gate`，而是：

1. `evidence/review-verdict.json`
2. `knowledge/output-policies/advice-output-policy.json`

这一步的流程意义很大：

- readiness 与 advice authority 被分开了
- 不再用“已经差不多 ready 了”偷渡“可以给高权威改稿建议”
- 输出面终于有了明确的 policy 边界

## 工具全景

### 1. 工作区初始化与基础检查

当前最关键的基础工具有三类：

1. `scripts/init_review_workspace.py`
   - 初始化标准 paper workspace、runtime pack、notes 和 evidence 结构
2. `scripts/check_review_workspace.py`
   - 做结构、gate、越权推进和模板污点检查
3. `scripts/evaluate_review_toolchain.py`
   - 输出结构化工具链评估结果

这三者共同定义了一个现实：

- 本项目不是“看完文档自己搭目录”
- 而是有初始化、检查、评估三段式工具骨架

### 2. 对象层与证据抽取工具

本项目当前已经把图、表、引文和形式项从“手工记忆”推进到了半结构化对象层：

1. `scripts/extract_docx_media.py`
2. `scripts/extract_docx_tables.py`
3. `scripts/extract_docx_citations.py`
4. `scripts/check_docx_formal_rules.py`

这套工具链的意义不是自动化本身，而是：

- 让图件、表格、引文和格式机械项从自然语言台账中独立出来
- 让后续审查可以回到对象层，而不是反复人工翻文档

### 3. 渲染与视觉证据工具

本项目对视觉证据的处理不是“只信 PDF”，而是更强调 authoring truth 与 visual truth 的区分。

这一点主要落在：

1. `scripts/render_docx_with_word.py`
2. `scripts/word_export_daemon.py`
3. `scripts/export_docx_to_pdf_word.jxa`
4. `scripts/word_export_worker.jxa`

这条工具链反映出的工具观是：

- Word/WPS 原稿和其视觉渲染不能混成一个层
- page renders 是审计资产，不是默认唯一事实
- 所见即所得编辑器需要一条受控渲染链，而不是简单扔给任意 PDF 快照

### 4. 路由、gate 与共享控制工具

当前 repo 已经有一组共享控制脚本，而不再把所有控制逻辑散在大脚本里：

1. `scripts/workflow_route_registry.py`
2. `scripts/release_gate_utils.py`
3. `scripts/specialty_gate_utils.py`
4. `scripts/output_policy_utils.py`
5. `scripts/runtime_pack_utils.py`
6. `scripts/evidence_workspace_utils.py`
7. `scripts/legacy_asset_paths.py`

这说明项目工具观已经从“一个脚本把所有事做完”升级成了“控制语义拆出来，给不同入口复用”。

### 5. 回归、评测与 validator

当前项目已经形成了两种不同的验证面：

1. 静态 acceptance / topic coverage
   - `scripts/validate_evals.py`
   - `evals/evals.json`
2. 执行型 workflow regression
   - `scripts/run_workflow_regression.py`
   - `evals/workflow_fixtures/*.json`

此外还依赖上游 `files-driven` 的 governed-pack validator 做 pack 级校验。

这套工具布局背后的方法很明确：

- 静态评测负责主题覆盖
- fixture runner 负责代表性流程链路
- 外部 validator 负责合同形状与 pack 正当性

### 6. 当前工具状态

我在本地补跑了一轮当前工具链，得到的事实是：

- `python3 scripts/validate_evals.py` 当前通过，验证 `50` 条 eval
- 当前仓库里有 `9` 条 workflow fixtures
- `python3 scripts/run_workflow_regression.py` 当前已全绿，`9/9` 通过
- `runtime_pack_drift_is_reported` 这条 fixture 已在本轮收平

这说明当前工具链已经具备较强的回归意识，但也说明：

- “项目已经进入稳定可维护状态”这个判断基本成立
- 关键 regression 已经进入可复跑、可定位、可修平的状态

## 方法全景

### 1. 主方法轴：`criteria -> evidence -> verdict -> advice`

本项目最值得保留的方法论主轴，不是 agent 数量，也不是 workflow 图多复杂，而是：

1. `criteria`
2. `evidence`
3. `verdict`
4. `advice`

它把项目从“给一段答案”转成了“先有判断边界，再有建议权”。

这也是当前项目相对成熟的根本原因。

### 2. 对象层优先于 Markdown 记忆

当前项目的方法观已经接受一个很关键的现实：

- 图、表、引文、资产路径、render chain 都不该长期只活在自然语言说明里

因此才会出现：

- `objects/figures.json`
- `objects/tables.json`
- `objects/citations.json`
- `objects/assets_manifest.json`

这不只是“方便机器处理”，更是为了降低接手成本和事实漂移。

### 3. 导师建议必须先经过方法学修复路径

本项目当前的导师式输出，并不是“批评后顺手给润色版”，而是要求先产出方法学修复路径。

固定链路已经比较清楚：

1. 问题归类
2. 修复类型
3. 方法学依据
4. 最小动作
5. 验证动作

然后才进入：

- `L1-L4` 分层
- `epi_basis / stats_basis / claim_boundary / mentor_action`
- role / specialty projection

这意味着当前方法观已经不再接受“更像导师的话术”替代“可验证的修复链条”。

### 4. 输出授权政策化

当前方法观的另一个成熟点是：

- 高风险输出不再靠上下文感觉或 release gate 文案放行
- 而是由 `review-verdict + advice-output-policy` 决定

这让“能不能改、能改到什么强度、哪些变换绝对禁止”第一次有了明确政策边界。

### 5. 热冷路径不是删能力，而是控制默认激活面

本项目对热冷路径的处理方法比较克制：

- 热路径只保留每次都要用的判断骨架
- 冷路径不是被删除，而是按论文类型、用户意图、输出形态、风险等级加载

这比“把所有高级能力默认堆上去”更接近真实使用场景。

### 6. 上下文隔离与条件化接手

本项目在方法层还形成了两条比较好的经验：

1. `process_projection` 只在需要 handoff 时强制
2. 上下文隔离复盘是为了把稳定结论和失效路径拆开，而不是为了写更多总结文件

这说明项目已经开始有意识地区分：

- 运行真源
- 过程投影
- 反思材料

## 经验全景

### 1. 已被证明有效的经验

以下经验已经基本被仓库事实证明有效：

1. 真源分层比补说明更重要
2. gate 进脚本比 gate 写在文档里更重要
3. 对象层结构化比手工台账更稳
4. 输出授权政策化比 readiness 文案更稳
5. 默认热路径收窄，比默认展开更多模块更利于稳定使用
6. workflow regression 必须和静态 eval 分工，而不是互相冒充

### 2. 已被证明代价很高的弯路

以下路径已经在本项目里被反复证明不该再走：

1. 用 coverage 标签替代真实 workflow 审计
2. 用 projection 代替 gate 推进阶段
3. 用对话记忆代替结构化真源
4. 在执行层不消费的前提下，先把字段写进合同
5. 让 `reviews/` 继续扮演唯一 authority 中心
6. 把 skill 维护流程和论文审查流程混成同一条主线

### 3. 当前最成熟的项目经验

如果只提炼一条最有价值的项目经验，那就是：

**这个项目真正的升级，不来自“加更多能力”，而来自“把已有能力收回到清晰的 authority、gate、policy 和 regression 上”。**

## Assessment

- 按当前仓库真源看，本项目的流程、工具、方法已经形成了相互咬合的一套体系，不再是散点能力集合。
- 如果用更严格的话说，当前项目已经完成了从“说明型 skill”到“受控型 thesis-review system”的第一代跃迁。
- 当前最稳的部分不是领域知识本身，而是控制面：双 workflow、runtime pack、evidence/verdict、policy-driven outputs、热冷路径、对象层工具链。
- 当前还不能说已经彻底 greenfield 化，因为知识真源仍主要放在 `references/`，还没有像 greenfield 设计那样彻底重构成独立 `knowledge/criteria` 与 `knowledge/evidence-models` 家族。

## Residual Risk

### 1. 知识真源仍偏解释层

当前项目已经把控制面做得很强，但知识面仍主要靠 `references/*.md` 承载。

这带来的风险是：

- 解释层和标准层仍有混层可能
- 后续如果继续扩专项与 criteria，`references/` 容易再次承担过多真源责任

### 2. macOS / Word 渲染依赖仍是现实边界

当前视觉证据链明显偏向 macOS + Word 原生导出路线。

这本身是 pragmatic 的选择，但会带来：

- 跨平台迁移成本
- Windows/Linux 复刻成本
- 环境不可得时的 fallback 边界问题

### 3. 根目录审计材料仍偏多

虽然 repo 级 workflow 已经分家，但根目录里仍有多份审计、复盘、计划和进度文件并存。

这不会直接破坏主流程，但会带来一个治理面的轻度风险：

- 新接手者可能一时分不清哪些是当前真源，哪些是过程性收敛材料

## Next

如果继续推进，这个项目下一阶段最值得做的不是再扩一轮新能力，而是收三件事：

1. 决定知识层是否继续 greenfield 化
   - 尤其是 `criteria / evidence models / output policies / templates` 是否要从 `references/` 进一步独立
2. 收根目录治理材料
   - 把计划、进度、审计与设计说明的落点再做一次减面，避免 repo 自身治理资产重新膨胀
3. 决定 runtime pack truth slice 是否继续扩到更多非 JSON 边界资产
   - 当前已把 `BOUNDARY.md` 纳入 snapshot 与 drift 检查；后续是否继续把更多 pack 根资产纳入，要按实际 validator 需求控制，不宜重新膨胀

## 一句话结论

本项目的“流程、工具、方法、经验”已经不是松散组合，而是围绕 `review-first`、对象层、政策化输出授权和双 workflow 分家逐步收成了一套受控系统；本轮又把 runtime pack validator / drift 这条回归边界补平了，剩下的主要问题已经回到知识真源和治理减面，而不是主控制链失效。
