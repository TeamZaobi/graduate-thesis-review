# 流程工具方法经验全景盘点短稿

这是一版适合对上游或外部对话直接转述的收口稿，只保留当前项目真源里已经稳定成立的结论，不展开新的总论。

## 收口结论

当前项目已经不是“一个会审论文的 prompt 集合”，而是围绕 `review-first`、对象层、政策化输出授权和双 workflow 分家逐步收成的一套受控系统。`workflow/review-workspace/` 与 `workflow/skill-maintenance/` 的分离，说明 repo 级评审流程和 skill 维护流程已经明确拆开，不能再混成一条主线叙述。

## 当前主轴

项目真正稳定的不是“多说几个专家角色”，而是四件事：

1. 先做高水平评审，再进入导师职责
2. 先建立对象层和证据层，再做结论与建议
3. 输出是否可放行，由 `review-verdict + advice-output-policy` 决定
4. 默认热路径收窄，冷路径按条件触发

这意味着本项目的中心能力已经从“给答案”转成“先收证据边界，再给可执行建议”。

## 工具链事实

当前工具链的作用已经超出“提取材料”，而是承担了初始化、检查、对象抽取、渲染、路由、gate、policy 和回归验证这些控制职责。实际可见的工具分工包括：

1. 工作区初始化与检查
2. 图表、引文、表格和形式项的对象化抽取
3. Word 原生渲染与视觉证据链
4. 路由、gate、policy、runtime pack 的共享控制
5. 静态 eval 与执行型 workflow regression 的分工

## 当前状态

本地当前核对结果是：

1. `python3 scripts/validate_evals.py` 通过，覆盖 `50` 条 eval
2. `python3 scripts/run_workflow_regression.py` 通过，`9/9` 全绿
3. 之前的 `runtime_pack_drift_is_reported` 已在本轮修平

这说明当前主控制链、runtime pack snapshot 和 workflow regression 的关系已经重新对齐。当前剩余问题更多是知识真源和治理减面，而不是这条回归边界本身。

## 一句话结论

这个项目的经验已经收敛为：真正有效的升级，来自把能力收回到清晰的 authority、gate、policy 和 regression 上，而不是继续扩展新角色或新叙述。
